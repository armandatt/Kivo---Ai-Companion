{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema packages/db/prisma/schema.prisma && npm run build --workspace api"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema packages/db/prisma/schema.prisma && npm run start --workspace api",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
